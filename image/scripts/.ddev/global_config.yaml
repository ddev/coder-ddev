# DDEV global configuration for Coder workspaces
# ddev-router default: enabled (omit_containers: []).
# Templates that don't use the router (e.g. ddev-drupal-core) override this
# at startup with: ddev config global --omit-containers=ddev-router
omit_containers: []
